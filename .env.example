# Database (relative path in dev)
DATABASE_URL=local.db

# Session
# Generate a strong secret with: openssl rand -hex 32
APP_SESSION_SECRET=
APP_SESSION_TTL=2592000

# Port the Node server listens on
PORT=3000
